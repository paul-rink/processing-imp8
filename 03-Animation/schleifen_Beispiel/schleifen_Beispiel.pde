int z = 0;

while (z < 3) { //A1: Verändere die Bedingung in der Schleife um die Zahlen von 0 bis 10 in der Konsole auszugeben.
  println(z);
  z = z + 1; //A2: Verändere die Zählbedingung, so dass nur jede zweite Zahl ausgegeben wird.
}
